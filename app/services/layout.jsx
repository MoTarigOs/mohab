export const metadata = {
    title: "our services",
    description: "We provide services like Printing, Designing & advertising"
};

const Layout = ({ children }) => {
  return (
    
    <main>{children}</main>

  )
};

export default Layout;
